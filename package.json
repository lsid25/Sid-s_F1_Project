{
  "name": "f1-2026-predictor",
  "version": "1.0.0",
  "description": "F1 2026 Telemetry & Finish Predictor — Full-stack monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"NEXT,FASTAPI\" --prefix-colors \"cyan,magenta\" \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && source venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "install:all": "npm install && cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "lint": "cd frontend && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
